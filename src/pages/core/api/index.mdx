---
title: Overview
description: Complete SignalX API reference
layout: docs
category: Reference
order: 10
---

# API Reference

Complete reference for all SignalX (`sigx`) exports. SignalX re-exports everything from:
- `@sigx/reactivity` - Reactive primitives
- `@sigx/runtime-core` - Component system
- `@sigx/runtime-dom` - DOM rendering

## Quick Navigation

### Reactivity
- [`signal`](/core/api/reactivity#signal) - Create reactive state
- [`effect`](/core/api/reactivity#effect) - Run side effects
- [`computed`](/core/api/reactivity#computed) - Derived values
- [`watch`](/core/api/reactivity#watch) - Watch with callbacks
- [`batch`](/core/api/reactivity#batch) - Batch updates
- [`untrack`](/core/api/reactivity#untrack) - Read without tracking
- [`toRaw`](/core/api/reactivity#toraw) - Get raw object
- [`isReactive`](/core/api/reactivity#isreactive) - Check if reactive
- [`effectScope`](/core/api/reactivity#effectscope) - Group effects
- [`toSignal`](/core/api/reactivity#tosignal) - Signal-shaped view over one property
- [`toSignals`](/core/api/reactivity#tosignals) - Signal-shaped views for every property

### Components
- [`component`](/core/api/components#component) - Define component
- [`Fragment`](/core/api/components#fragment) - Render without wrapper
- [`lazy`](/core/api/components#lazy) - Lazy load component
- [`Defer`](/core/api/components#defer) - Chunk-loading boundary
- [`Portal`](/core/api/components#portal) - Render to different DOM location
- [`mergeProps`](/core/api/components#mergeprops) - Combine prop sources without collisions

### Data Loading
- [`useData`](/core/api/components#usedata) - Keyed async read, SSR-transferable
- [`useAction`](/core/api/components#useaction) - Async write, triggered by `run()`
- [`all`](/core/api/components#all) - Combine reads into one state
- [`useStream`](/core/api/components#usestream) - Progressive text streaming
- [`errorScope`](/core/docs/error-handling) - Subtree error boundary

### Lifecycle
- [`onMounted`](/core/api/lifecycle#onmounted) - After mount
- [`onUnmounted`](/core/api/lifecycle#onunmounted) - Before unmount
- [`onCreated`](/core/api/lifecycle#oncreated) - After creation
- [`onUpdated`](/core/api/lifecycle#onupdated) - After update

### Dependency Injection
- [`provide`](/core/api/di#provide) - Provide value to descendants
- [`inject`](/core/api/di#inject) - Inject from ancestors
- [`createInjectable`](/core/api/di#createinjectable) - Create injectable
- [`defineProvide`](/core/api/di#defineprovide) - Provide + create

### App & Plugins
- [`defineApp`](/core/api/app#defineapp) - Create application
- [`Plugin`](/core/api/app#plugin) - Plugin type

### Advanced
- [`defineFactory`](/core/api/advanced#definefactory) - Injectable factory with real lifetimes
- [`Lifetime`](/core/api/advanced#lifetime) - `'singleton' | 'scoped' | 'transient'`
- [`createTopic`](/core/api/advanced#createtopic) - Pub/sub messaging
- [`createTopicGroup`](/core/api/advanced#createtopicgroup) - Typed group of topics
- [`toSubscriber`](/core/api/advanced#tosubscriber) - Read-only subscriber

### Types
- [`Define.Prop`](/core/api/types#defineprop) - Prop type helper
- [`Define.Event`](/core/api/types#defineevent) - Event type helper
- [`Define.Slot`](/core/api/types#defineslot) - Slot type helper
- [`Define.Model`](/core/api/types#definemodel) - Two-way binding helper
- [`Define.Expose`](/core/api/types#defineexpose) - Exposed API helper
- [`Define.Attrs`](/core/api/types#defineattrs) - Accept host attributes
- [`Define.WithAttrs`](/core/api/types#definewithattrs) - Host attributes with a colliding own prop
- [`ComponentRef`](/core/api/types#componentref) - Component ref type
- [`Exposed`](/core/api/types#exposed) - Extract exposed type

## Import

All APIs are available from the main `sigx` package:

```tsx
import { 
    // Reactivity
    signal, effect, computed, watch,
    batch, untrack, toRaw, isReactive, effectScope,
    toSignal, toSignals,
    
    // Components
    component, Fragment, lazy, Defer, Portal,
    
    // Data loading
    useData, useAction, all, useStream, errorScope,
    
    // Lifecycle
    onMounted, onUnmounted, onCreated, onUpdated,
    
    // DI
    provide, inject, createInjectable, defineProvide,
    
    // App
    defineApp,
    
    // Types
    type Define, ComponentRef, Exposed,
    
    // Advanced
    defineFactory, createTopic, createTopicGroup, toSubscriber,
    type Lifetime
} from 'sigx';
```

The inspection-only topic registry for tooling lives on its own entry:

```tsx
import { getTopic, listTopics, subscribeTopics, onTopicCreated } from '@sigx/runtime-core/inspect';
```

## TypeScript

SignalX is written in TypeScript and provides full type inference. All component props, events, slots, and exposed APIs are fully typed.

```tsx
// Full type safety
type MyProps = 
    & Define.Prop<'name', string, true>
    & Define.Prop<'count', number>
    & Define.Event<'change', number>
    & Define.Slot<'header'>
    & Define.Expose<{ reset: () => void }>;

const MyComponent = component<MyProps>(({ props, emit, slots, expose }) => {
    // props.name is string (required)
    // props.count is number | undefined (optional)
    // emit('change', 5) is type-checked
    // slots.header is typed correctly
    
    expose({ reset: () => {} }); // Type-checked
    
    return () => <div>{props.name}</div>;
});
```
