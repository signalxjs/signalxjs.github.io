---
title: "Installation"
description: "Install and configure @sigx/runtime-dom, the Runtime DOM package for SignalX."
layout: module-docs
category: "Getting Started"
order: 20
---

# Installation

<p class="lede">Add `@sigx/runtime-dom` to your project.</p>

## Install the package

```bash
pnpm add @sigx/runtime-dom
```

## Verify

```tsx
import * as RuntimeDOM from '@sigx/runtime-dom';

console.log(Object.keys(RuntimeDOM));
```
