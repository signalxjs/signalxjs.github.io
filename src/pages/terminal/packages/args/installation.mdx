---
title: "Installation"
description: "Args — Fluent, type-aware argument parser"
layout: module-docs
category: "Getting Started"
order: 20
---

# Installation

<p class="lede">Add `@sigx/args` to your project.</p>

## Install the package

```bash
pnpm add @sigx/args
```

## Verify

```tsx
import * as Args from '@sigx/args';

console.log(Object.keys(Args));
```
