---
title: "Badge"
description: "A small labelled tag in solid, accent or bracket styles"
layout: docs
category: ["Components", "Feedback"]
order: 122
---

# Badge

<p class="lede">A small labelled tag — a status pill or count. Three styles via <code>variant</code>.</p>

## Import

```tsx
import { Badge } from '@sigx/terminal';
```

## Usage

```tsx
import { component, defineApp, Badge } from '@sigx/terminal';

const App = component(() => {
    return () => (
        <box>
            <Badge label="NEW" variant="solid" color="accent" />
            <text> </text>
            <Badge label="beta" variant="accent" color="info" />
            <text> </text>
            <Badge label="3" variant="bracket" color="warn" />
        </box>
    );
});

defineApp(<App />).mount({ clearConsole: true });
```

## Props

| Prop | Type | Notes |
| --- | --- | --- |
| `label` | `string` | **required** — the badge text |
| `variant` | `'solid' \| 'accent' \| 'bracket'` | visual style |
| `color` | token \| `#hex` | badge color |

## See also

- [StatusBar](/terminal/docs/components/statusbar) — a row of keyed status chips.
