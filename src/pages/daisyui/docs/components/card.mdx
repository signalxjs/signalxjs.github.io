---
title: Card
description: Flexible container component for grouping related content
layout: docs
category: ["Components", "Data Display"]
order: 230
---

# Card

Cards are flexible containers for grouping related content and actions.

## Import

```tsx
import { Card } from '@sigx/daisyui';
```

## Basic Usage

```tsx preview code live
import { component, render } from 'sigx';
import { Card, Button, Text } from '@sigx/daisyui';

const Demo = component(() => {
    return () => (
        <Card width="80">
            <Card.Body>
                <Card.Title>Card Title</Card.Title>
                <Text as="p">This is a basic card with some content.</Text>
                <Card.Actions>
                    <Button variant="primary">Action</Button>
                </Card.Actions>
            </Card.Body>
        </Card>
    );
});

render(<Demo />, "#sandbox");
```

## With Image

```tsx preview code live
import { component, render } from 'sigx';
import { Card, Button, Text } from '@sigx/daisyui';

const Demo = component(() => {
    return () => (
        <Card width="80">
            <Card.Image 
                src="https://picsum.photos/400/200" 
                alt="Random image"
                class="w-full h-48 object-cover"
            />
            <Card.Body>
                <Card.Title>Photo Card</Card.Title>
                <Text as="p">A card with an image at the top.</Text>
                <Card.Actions>
                    <Button variant="primary" size="sm">View</Button>
                </Card.Actions>
            </Card.Body>
        </Card>
    );
});

render(<Demo />, "#sandbox");
```

## Variants

### Bordered

```tsx preview code live
import { component, render } from 'sigx';
import { Card, Text } from '@sigx/daisyui';

const Demo = component(() => {
    return () => (
        <Card bordered width="80">
            <Card.Body>
                <Card.Title>Bordered Card</Card.Title>
                <Text as="p">This card has a visible border.</Text>
            </Card.Body>
        </Card>
    );
});

render(<Demo />, "#sandbox");
```

### Compact

```tsx preview code live
import { component, render } from 'sigx';
import { Card, Text } from '@sigx/daisyui';

const Demo = component(() => {
    return () => (
        <Card variant="compact" width="80">
            <Card.Body>
                <Card.Title>Compact Card</Card.Title>
                <Text as="p">Less padding for compact layouts.</Text>
            </Card.Body>
        </Card>
    );
});

render(<Demo />, "#sandbox");
```

## Card Grid

```tsx preview code live
import { component, render } from 'sigx';
import { Card, Button, Col, Row, Text } from '@sigx/daisyui';

const Demo = component(() => {
    const items = [
        { title: 'Project Alpha', desc: 'A cutting-edge web application' },
        { title: 'Project Beta', desc: 'Mobile-first design system' },
        { title: 'Project Gamma', desc: 'Backend API service' },
    ];
    
    return () => (
        <Row gap="4" wrap>
            {items.map(item => (
                <Card bordered width="72">
                    <Card.Body>
                        <Col gap="3">
                            <Card.Title>{item.title}</Card.Title>
                            <Text color="muted">{item.desc}</Text>
                            <Card.Actions>
                                <Button size="sm">Details</Button>
                            </Card.Actions>
                        </Col>
                    </Card.Body>
                </Card>
            ))}
        </Row>
    );
});

render(<Demo />, "#sandbox");
```

## Props

### Card Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `variant` | `'normal' \| 'compact' \| 'side' \| 'bordered' \| 'image-full'` | `'normal'` | Card style variant |
| `shadow` | `boolean \| 'sm' \| 'md' \| 'lg' \| 'xl'` | `true` | Shadow style |
| `bordered` | `boolean` | `false` | Add border style (`card-bordered`) |
| `glass` | `boolean` | `false` | Glassmorphism style |
| `imageFull` | `boolean` | `false` | Full-image overlay style |
| `bgColor` | `string` | `'bg-base-100'` | **Deprecated** — prefer `background`. Raw bg utility class |
| `background` | `BackgroundColor` | - | Theme-aware background, e.g. `"base-200"` |
| `rounded` | `RadiusValue \| boolean` | - | Corner rounding (`true` → theme `rounded-box`) |
| `width` | `SizeValue` | - | Tailwind step (`"56"`=14rem/224px) or unitful value (`"560px"`, `"50%"`) |
| `height` | `SizeValue` | - | Same value format as `width` |
| `padding` | `Spacing` | - | Inner padding |
| `class` | `string` | `undefined` | Additional CSS classes |

### Card.Body Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `center` | `boolean` | `false` | Center content horizontally |
| `class` | `string` | `undefined` | Additional CSS classes |

### Card.Title Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `class` | `string` | `undefined` | Additional CSS classes |

### Card.Actions Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `justify` | `'start' \| 'center' \| 'end'` | `'end'` | Actions alignment |
| `class` | `string` | `undefined` | Additional CSS classes |

### Card.Image Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `src` | `string` | - | Image source URL |
| `alt` | `string` | `''` | Alt text |
| `class` | `string` | `undefined` | Additional classes for the `figure` wrapper |
