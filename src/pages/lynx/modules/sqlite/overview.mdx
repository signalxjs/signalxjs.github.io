---
title: "Overview"
description: "SQLite — Embedded SQLite database"
layout: module-docs
category: "Getting Started"
order: 10
---

import { PkgVersion } from '@/components/PkgVersion';

# SQLite

<p class="lede">SQL, transactions, migrations and live queries for offline-first apps — openDatabase, parameterized execute, PRAGMA user_version migrations and a reactive useLiveQuery hook.</p>

<p class="mod-meta-row"><PkgVersion npm="@sigx/lynx-sqlite" /> <span class="mmr-dot" /> iOS · Android <span class="mmr-dot" /> MIT</p>

## Installation

```bash
pnpm add @sigx/lynx-sqlite
sigx prebuild   # auto-links the native module
```

## About

`@sigx/lynx-sqlite` is a Lynx module — embedded SQLite database. It is versioned in lockstep with the rest of the Lynx module family, so any combination of modules just works together.

## Next steps

Continue with [Installation](/lynx/modules/sqlite/installation) for project setup, or jump to the [API reference](/lynx/modules/sqlite/api).
