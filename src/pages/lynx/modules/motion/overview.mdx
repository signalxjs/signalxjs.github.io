---
title: "Overview"
description: "Animation drivers — the Motion module for SignalX Lynx (@sigx/lynx-motion): what it does, setup and API."
layout: module-docs
category: "Getting Started"
order: 10
---

import { PkgVersion } from '@/components/PkgVersion';

# Motion

<p class="lede">Animation drivers for SignalX Lynx — withSpring, withTiming and animate, running on the main thread with progress observable from the background thread for free.</p>

<p class="mod-meta-row"><strong>29k</strong> weekly <span class="mmr-dot" /> <PkgVersion npm="@sigx/lynx-motion" /> <span class="mmr-dot" /> iOS · Android <span class="mmr-dot" /> MIT</p>

## Installation

```bash
pnpm add @sigx/lynx-motion
```

## Animate a shared value

```tsx
import { useSharedValue, useAnimatedStyle } from '@sigx/lynx';
import { withSpring } from '@sigx/lynx-motion';

const scale = useSharedValue(1);

const style = useAnimatedStyle(() => ({
    transform: [{ scale: scale.value }],
}));

const onPress = () => { scale.value = withSpring(1.2); };
```

The spring solves on the main thread — no bridge round-trips per frame — while `scale.value` stays readable from your components like any signal.

## Drivers

- **withSpring** — physics-based; configure mass, stiffness, damping
- **withTiming** — duration + easing curves
- **animate** — imperative sequences (`await animate(...)`)

## With gestures

Motion and [Gestures](/lynx/modules/gestures/overview) share `SharedValue`s, so a pan can hand off into a spring with zero glue:

```tsx
const pan = usePan((e) => { x.value = e.translationX; })
    .onEnd(() => { x.value = withSpring(0); });
```

## Next steps

See [Usage](/lynx/modules/motion/usage) for sequencing and interruption, or the [API reference](/lynx/modules/motion/api).
