---
title: "Installation"
description: "Install and configure @sigx/lynx-network, the Network module for SignalX Lynx."
layout: module-docs
category: "Getting Started"
order: 20
---

# Installation

<p class="lede">Add `@sigx/lynx-network` to your project.</p>

## Install the package

```bash
pnpm add @sigx/lynx-network
```

## Native setup

Native modules are autolinked by `@sigx/lynx-cli` — run a prebuild after installing:

```bash
sigx prebuild
```

## Verify

```tsx
import * as Network from '@sigx/lynx-network';

console.log(Object.keys(Network));
```
