---
title: "Installation"
description: "Markdown — Streaming markdown renderer"
layout: module-docs
category: "Getting Started"
order: 20
---

# Installation

<p class="lede">Add `@sigx/lynx-markdown` to your project.</p>

## Install the package

```bash
pnpm add @sigx/lynx-markdown
```

## Native setup

Rebuild your development client after installing:

```bash
sigx prebuild
```

## Verify

```tsx
import * as Markdown from '@sigx/lynx-markdown';

console.log(Object.keys(Markdown));
```
