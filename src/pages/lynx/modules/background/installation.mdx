---
title: "Installation"
description: "Install and configure @sigx/lynx-background, the Background Tasks module for SignalX Lynx."
layout: module-docs
category: "Getting Started"
order: 20
---

# Installation

<p class="lede">Add `@sigx/lynx-background` to your project.</p>

## Install the package

```bash
pnpm add @sigx/lynx-background
```

## Native setup

Native modules are autolinked by `@sigx/lynx-cli` — run a prebuild after installing:

```bash
sigx prebuild
```

## Verify

```tsx
import * as BackgroundTasks from '@sigx/lynx-background';

console.log(Object.keys(BackgroundTasks));
```
