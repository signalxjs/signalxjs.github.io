---
title: "Overview"
description: "Background Tasks — Periodic sync & fetch"
layout: module-docs
category: "Getting Started"
order: 10
---

import { PkgVersion } from '@/components/PkgVersion';

# Background Tasks

<p class="lede">Schedule periodic background sync and fetch (BGTaskScheduler / WorkManager).</p>

<p class="mod-meta-row"><PkgVersion npm="@sigx/lynx-background" /> <span class="mmr-dot" /> iOS · Android <span class="mmr-dot" /> MIT</p>

## Installation

```bash
pnpm add @sigx/lynx-background
sigx prebuild   # auto-links the native module
```

## About

`@sigx/lynx-background` is a Lynx module — periodic sync & fetch. It is versioned in lockstep with the rest of the Lynx module family, so any combination of modules just works together.

## Next steps

Continue with [Installation](/lynx/modules/background/installation) for project setup, or jump to the [API reference](/lynx/modules/background/api).
