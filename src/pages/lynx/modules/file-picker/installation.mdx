---
title: "Installation"
description: "File Picker — Document picker"
layout: module-docs
category: "Getting Started"
order: 20
---

# Installation

<p class="lede">Add `@sigx/lynx-file-picker` to your project.</p>

## Install the package

```bash
pnpm add @sigx/lynx-file-picker
```

## Native setup

Native modules are autolinked by `@sigx/lynx-cli` — run a prebuild after installing:

```bash
sigx prebuild
```

## Verify

```tsx
import * as FilePicker from '@sigx/lynx-file-picker';

console.log(Object.keys(FilePicker));
```
