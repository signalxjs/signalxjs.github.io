---
title: "Actors packages - backends, transports & tooling for @sigx/actors"
description: "Browse the actors packages — Redis, Postgres, SurrealDB and Kubernetes providers for membership, directory and storage; TCP and WebSocket transports; the Cloudflare Durable Objects backend; the CLI and web dashboards with their shared monitor data layer; and the OpenTelemetry exporters."
layout: package
---

import { ModuleCatalog } from '@/components/ModuleCatalog';

{/*
  Actors package catalog — a chrome page, not a collection. Cards come from the
  module registry; links resolve into each package's own docs collection.

  MDX rather than .tsx for the draft gate, and the comment sits below the
  import — see the note in ../index.mdx for both reasons.
*/}

<ModuleCatalog parent="actors" />
