---
title: "Actors packages - backends, transports & tooling for @sigx/actors"
description: "Browse the actors packages — Redis, Postgres and Kubernetes providers for membership, directory and storage; TCP and WebSocket host transports; the Cloudflare Durable Objects backend; and the CLI dashboard and OpenTelemetry exporters."
layout: package
draft: true
---

{/*
  Actors package catalog — a chrome page, not a collection. Cards come from the
  module registry; links resolve into each package's own docs collection.

  MDX rather than .tsx for the draft gate — see the note in ../index.mdx.
*/}

import { ModuleCatalog } from '@/components/ModuleCatalog';

<ModuleCatalog parent="actors" />
