---
title: "Overview"
description: "Cloudflare — One Durable Object per actor"
layout: module-docs
category: "Getting Started"
order: 10
draft: true
---

import { PkgVersion } from '@/components/PkgVersion';

# Cloudflare

<p class="lede">Cloudflare already guarantees one instance of a Durable Object globally and serializes its requests — which is the virtual-actor contract. So there is no membership, no directory and no authenticated host mount: the platform is the cluster.</p>

<p class="mod-meta-row"><PkgVersion npm="@sigx/actors-cloudflare" /> <span class="mmr-dot" /> MIT</p>

## Installation

```bash
pnpm add @sigx/actors-cloudflare
```

## About

`@sigx/actors-cloudflare` is a Actors package — one Durable Object per actor. It is versioned in lockstep with the rest of the actors repo, so any combination of backends, transports and tools just works together.

## Next steps

Continue with [Installation](/actors/packages/actors-cloudflare/installation) for project setup, or jump to the [API reference](/actors/packages/actors-cloudflare/api).
