---
title: "API reference"
description: "Netlify — Frameworks API function generation"
layout: module-docs
category: "Reference"
order: 40
---

import { PkgVersion } from '@/components/PkgVersion';

# API reference

<p class="lede">Exports of `@sigx/netlify` <PkgVersion npm="@sigx/netlify" />.</p>

## netlify

Create the Netlify adapter for `sigx({ ssr: { adapter } })` — a fully bundled
function emitted through the Frameworks API
(`.netlify/v1/functions/sigx-ssr/`), with `preferStatic` catch-all routing.

```ts
function netlify(options?: NetlifyAdapterOptions): SigxAdapter

interface NetlifyAdapterOptions {
  /** Platform entry (project-relative), scaffolded iff absent. Default 'src/entry.netlify.ts'. */
  entry?: string;
}
```

See [Deploying to Netlify](/deploy/packages/netlify/deploying) for the
generated layout and routing policy.
